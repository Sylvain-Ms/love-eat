<%= simple_form_for [conversation, message], html: { class: 'form_chat', data: { controller: "form-chat" }} do |f| %>
  <%= f.input :content, label: false, input_html: { class: "input_content"} %>
  <%= image_tag 'send.png', class: 'button-nul', data: { action: "click->form-chat#send" } %>
<% end %>
