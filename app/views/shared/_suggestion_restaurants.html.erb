<div>

    <li></li>
    <li></li>
    <li></li>
  </ul>
</div>
<div class="hamburger-menu">
    <input id="menu__toggle" type="checkbox" />
    <label class="menu__btn" for="menu__toggle">
      <span></span>
    </label>

    <ul class="menu__box" > <%= image_tag 'restaurant.png' %>
      <% @restaurants.each.where(restaurants = current_user.foodmoods && user_liked_id.foodmoods && restaurants.category) do |restaurant|%>
      <%= restaurant.name %>
      <%= restaurant.image %>
    </ul>
  </div>
